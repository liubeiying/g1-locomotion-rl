# 环境搭建

> 上游：legged_gym / rsl_rl / unitree_rl_gym

## 安装顺序

顺序不能乱，Isaac Gym 必须先装，而且要先用它自带的 example 验证过再往下走。

1. **Isaac Gym**：跑通自带 example（如 `1080_balls_of_solitude.py`）
2. **rsl_rl**：PPO 实现，legged_gym 对它的版本很敏感
3. **legged_gym**：训练框架
4. **unitree_rl_gym**：G1 配置 + MuJoCo / 真机部署链路

我一开始跳过了第 1 步的验证直接往后装，结果后面报错时分不清是环境问题还是代码问题，又倒回来重查，多花了半天。**这一步没过就别往下装。**

## 版本对照

前六行是上游安装文档给出的建议值，可直接对照；「我的实测」一列是我装完后实际固化的版本。

| 组件 | 上游建议值 | 来源 | 我的实测 |
|---|---|---|---|
| Ubuntu | 18.04 及以上 | unitree_rl_gym 安装文档 | 22.04 |
| NVIDIA 驱动 | 525+ | 同上 | 待补 |
| CUDA | 与 PyTorch 绑定（官方示例 cu113） | 同上 | 待补 |
| Python | 3.6 / 3.7 / 3.8，推荐 3.8 | 同上 | 待补 |
| PyTorch | 1.10 + cu113 | 同上 | 待补 |
| Isaac Gym | Preview 4 | 同上 | Preview 4 |
| rsl_rl | v1.0.2 | 同上 | 待补 |
| MuJoCo | 随 unitree_rl_gym 依赖安装 | 同上 | 待补 |

## 关键检查点

- [ ] Isaac Gym 自带 example 能跑
- [ ] `python -c "import isaacgym"` 无报错
- [ ] `train.py --task=g1 --headless` 能启动
- [ ] `nvidia-smi` 观察到的显存占用符合预期

## 关于并行环境数

社区经验值是每 1GB 显存约支撑 5–10 个并行环境，所以 `num_envs` 不能照抄教程——教程里的 4096 是按他们的卡给的。我的做法是先用小值跑通，再往上加，加到 OOM 之前一档就是这台机器的上限。具体数值待补。
